{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8788",
    "build": "wrangler build",
    "test": "vitest run",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@ai-sdk/google-vertex": "^3.0.28",
    "ai": "^5.0.48",
    "hono": "^4.8.9"
  },
  "devDependencies": {
    "@hono/zod-validator": "^0.7.2",
    "zod": "^4.1.8"
  }
}
